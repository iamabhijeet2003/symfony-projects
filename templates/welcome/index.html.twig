{% extends 'base.html.twig' %}

{% block title %}Hello WelcomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello! {{user}}✅</h1>

    <h2>{{ missatge }}</h2>
    {% if (language)  %}
        {% if (language=='va') %}
            <h1>Benvingut {{user}} </h1>
        {% elseif (language=='es') %}
            <h1>Bienvenido {{user}} </h1>
        {% elseif (language=='en') %}
            <h1>Welcome {{user}} </h1>
        {% else %}
            <h1>Sorry {{user}}, I dont speak your language </h1>
        {% endif %}
    {% endif %}
</div>


<div>
    <a href="/welcome/en/{{user}}">English</a><br>
    <a href="/welcome/es/{{user}}">Catellano</a><br>
    <a href="/welcome/va/{{user}}">Valencia</a><br>

    <a href="{{path('app_welcome_en',{user:user})}}">English</a><br>
    <a href="{{path('app_welcome_es',{user:user})}}">Catellano</a><br>
    <a href="{{path('app_welcome_va',{user:user})}}">Valencia</a>


<!--
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{path('app_welcome_en',{user:user})}}">English</a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{path('app_welcome_es',{user:user})}}">Castellano</a>
            </li>
            <li class="nav-item">
            <a class="nav-link" href="{{path('app_welcome_va',{user:user})}}">Valencia</a>
            </li>
            
        </ul>
        </div>
    </div>
    </nav>
-->

</div>
{% endblock %}
